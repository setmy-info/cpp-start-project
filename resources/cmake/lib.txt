# PLACEHOLDER-BEGIN #

INSTALL(DIRECTORY "${LIBRARY_OUTPUT_PATH}/xyz.so"                DESTINATION lib)

IF(DISTRIBUTION STREQUAL "FreeBSD")
    INSTALL(FILES "${LIBRARY_OUTPUT_PATH}/xyz"                DESTINATION lib)
ENDIF()

# PLACEHOLDER-END #
