# PLACEHOLDER-BEGIN #

INSTALL(PROGRAMS  "${BINARY_OUTPUT_PATH}/xyz"       DESTINATION bin)
INSTALL(PROGRAMS  "${BINARY_OUTPUT_PATH}/xyz.bin"   DESTINATION bin)

IF(DISTRIBUTION STREQUAL "FreeBSD")
    INSTALL(PROGRAMS  "${BINARY_OUTPUT_PATH}/xyz"   DESTINATION bin)
ENDIF()

IF (WIN32)
    # TODO : Win32/Win64 things
ENDIF()

# PLACEHOLDER-END #
